{
  "name": "quest-maker",
  "scripts": {
    "build": "mkdir -p dist && cp -r gfx dist && parcel build src/index.html",
    "watch": "parcel src/index.html"
  },
  "dependencies": {
    "glob": "^7.1.6",
    "pixi-plugin-bump": "^1.1.8",
    "pixi.js": "^5.3.3",
    "src": "^1.1.2"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "typescript": "^4.0.2"
  }
}
